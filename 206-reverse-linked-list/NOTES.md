-traverse the node iteratively.
-base case : when the temp is null then it return.
-when the iterative funtion return it change the temp->next to the previous one node adress.
​
### **another approach********
​
-traverse